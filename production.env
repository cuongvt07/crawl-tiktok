# Cấu hình môi trường production
FLASK_ENV=production
FLASK_DEBUG=0

# Thông số kết nối nếu cần
#DATABASE_URL=sqlite:////app/data/tiktokcrawler.db

# Cấu hình proxy mặc định (nếu cần)
#DEFAULT_PROXY=http://your-proxy-server:port
